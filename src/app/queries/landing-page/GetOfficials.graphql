query GetOfficials($locale: I18NLocaleCode) {
  officials(locale: $locale) {
    data {
      id
      attributes {
        institutionPhoto {
          data {
            attributes {
              url
            }
          }
        }
        institutionName
        institutionLink
      }
    }
  }
}
